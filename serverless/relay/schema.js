// No persistent state needed: this project only relays channel update events to
// RayNews over HTTP. Kept as an empty schema so `tgcloud push`/`migrate` have a
// valid (if trivial) schema file to work against.
